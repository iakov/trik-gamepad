<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat">
        <item name="colorPrimary">@color/greendark</item>
        <item name="colorPrimaryDark">@color/greendark</item>
        <item name="colorAccent">@color/greenlight</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- Support library compatibility -->
        <item name="windowActionBarOverlay">true</item>
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- Settings matches the main screen's dark full-screen look (the old Light
         variant clashed with the gamepad's dark immersive UI). -->
    <style name="Theme.AppCompat.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="colorPrimary">@color/greendark</item>
        <item name="colorPrimaryDark">@color/greendark</item>
        <item name="colorAccent">@color/greenlight</item>
    </style>

</resources>
