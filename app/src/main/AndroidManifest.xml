<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.screen.landscape" android:required="true" />
    <uses-feature android:name="android.hardware.touchscreen.multitouch.distinct" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />

    <!-- Package visibility: ReportSharer probes for text editors handling the
         report file (ACTION_EDIT + text/plain) to decide between the
         review-in-editor flow and the direct share sheet. Without this the
         query returns empty on API 30+ and the editor flow silently dies. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.EDIT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

  <!--  <uses-permission android:name="android.permission.VIBRATE" /> -->

    <application
        android:name=".App"
        android:icon="@mipmap/trik_gamepad_logo_512x512"
        android:label="@string/appLabel"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="n">
       <!--  <service android:name=".SenderService" />  -->

       <!-- Shares the diagnostic report file with other apps (text editor for
            review, or mail/IM for the final report); exported=false and limited
            to cacheDir/diagnostics via res/xml/file_paths.xml. -->
       <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

       <activity
            android:name="com.trikset.gamepad.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.trikset.gamepad.SettingsActivity"
            android:label="@string/menu_settings"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar.FullScreen">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
    </application>

</manifest>
